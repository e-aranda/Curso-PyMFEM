// Malla FreeFem para el ejemplo 2

border a(t=0, 2.*pi){x=cos(t); y=sin(t); label=0;};
border b(t=0, 2.*pi){x=0.6+0.3*cos(t); y=0.3*sin(t); label=1;};

mesh Th = buildmesh(a(50) + b(-30));
plot(Th); //plot Th then press Enter

savemesh(Th,"doblecirc.msh");

